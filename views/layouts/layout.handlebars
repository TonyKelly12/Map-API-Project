<!doctype html>
<html>
<head>
    {{>head}}
</head>
<body background="/img/svg/background.svg">
{{>navbar}}

{{>error}}

{{#if user}}
{{else}}
{{>jumbotron}}
{{/if}}
<div class="container-fluid">
    <div class="row">
        <div class="expressContent">
            {{{body}}}
        </div>
    </div>
</div>
</body>
</html>
